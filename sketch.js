sketch.js

function setup() {
    noCanvas();
    CountQueuingStrategy

}


class Counter {
    constructor(start, wait) {
        this.count = start;
        this.p = createP('');
        set
    }
}